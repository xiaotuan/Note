### 第9章　函数

**本章介绍以下内容：**

+ 关键字—— `return` ；
+ 运算符——*（一元）、 `&` （一元）；
+ 函数及其定义方式；
+ 如何使用参数和返回值；
+ 如何把指针变量用作函数参数；
+ 函数类型；
+ ANSI C原型；
+ 递归。

如何组织程序？C的设计思想是，把函数用作构件块。我们已经用过C标准库的函数，如printf()、scanf()、getchar()、putchar()和strlen()。现在要进一步学习如何创建自己的函数。前面章节中已大致介绍了相关过程，本章将巩固以前学过的知识并做进一步的拓展。

