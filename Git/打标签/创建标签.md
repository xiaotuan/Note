[toc]

### 1. 附注标签

可以使用 `git tag -a 标签名称 -m "标签附注"` 命令创建一个附注标签:

```shell
$ git tag -a v1.4 -m "my version 1.4"
```

如果在 `-m` 选项后面没有指定附注内容，Git 会运行编辑器要求你输入信息。

### 2. 轻量标签

创建轻量标签，不需要使用 `-a`、`-s` 或 `-m` 选项，只需要提供标签名字：

```shell
$ git tag v1.4-lw
```



