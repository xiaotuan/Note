### 7.7.1　问题

如果动画正在运行，就可能必须在中途停止它。常见的一个问题是在使用mouseover和mouseout事件触发动画显示和隐藏特定内容时发生的。

如果鼠标在触发区域进进出出，就会不断触发动画；例如，内容块不断地上下滑动，直到完成触发的次数。

解决的方法之一是使用 `:animated` 选择器过滤动画元素。但是，你可能希望在用户把鼠标移出触发区域时将元素淡出而不是完成动画。这可以用 `stop()` 方法解决。

