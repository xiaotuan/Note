### 7.0.2　动画速度

`speed` 参数可以用毫秒或者几种预定义字符串指定：

+ `slow` 表示600毫秒。
+ `fast` 表示200毫秒。

如果动画函数中没有显式指定速度值，动画将以400毫秒的默认速度运行。

注意

> 如果明确地传递速度值0，动画将和 `.css()` 函数一样运行，但是在jQuery 1.3版本中，该方法调用将同步运行，而不采用其他所有动画的异步方式。

