### 4.6.3　讨论

jQuery的 `$.unique()` 函数从数组或者集合中删除重复的DOM元素。在前一个秘诀中，用 `$.merge()` 合并了 `animals` 和 `horses` 数组。jQuery在大部分核心和内部函数（如 `.find()` 和 `.add()` )中使用了 `$.unique()` 。因此，这个方法的最常见用例是在不是由jQuery构造的数组上进行的操作。

