### 17.3.3　讨论

DOM Storage API由两个接口组成： `sessionStorage` 和 `localStorage` 。

Firefox从2.0版本起包含这个特性，当时标准还在开发中。之后，该标准经过了修改。Internet Explorer 8.0有当前API的一个实现。Safari和Firefox即将推出的版本也将遵循当前的规范。但是，Firefox 2.0～3.0浏览器还将持续一段时间。编写支持 `globalStorage` 的应用程序也能为这些遗留的浏览器服务。

