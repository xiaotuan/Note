### 4.1.6　运行应用程序

以开发模式运行应用程序的最简单的方法是运行：

```javascript
npm run dev
```

还可以使用其他命令，但最主要的命令是 `dev` 。要查看其他可用命令，可以运行：

```javascript
npm run
```

这会列出该仓库的所有可用命令。随便试试这些命令以便了解它们是如何满足项目需要的。我们重点关注的两个主要命令是 `npm run dev` 和 `npm run db:seed` 。

