### 第12章　页面内的元素调序

在目前的网页应用中，移动元素而后将它们插入到页面中别的位置几乎已成必不可少的功能。用户可以使用鼠标可视化地拖动元素，然后由系统自动地将被移动的元素插入到目标位置。jQuery UI提供了实现这一功能的机制，且这一切都是在其内部自动处理的。

本书中我们将这一操作称为“调序”（permutation）。这一操作将元素拖离原先的位置，同时扩大目标位置的空间以容纳元素的插入。

