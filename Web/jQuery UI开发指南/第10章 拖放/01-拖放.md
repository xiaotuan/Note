### 第10章　拖放

拖放（drag-and-drop）是网页上常见的一种操作，用来移动某个元素（使用鼠标将其放置到另一个页面元素上）。例如，页面上显示有一些待购买的商品的图片，用户可以直接将其拖曳到购物车中，形象地完成购物。

jQuery UI可以管理这些操作，并能对”拖曳”（移动某元素）和“放置”（搁放下刚刚移动的元素）加以区分。为此，jQuery UI同时提供了 `draggable ()` 和 `droppable ()` 两个方法。

