### 14.1　新的视觉特效

除了jQuery自带的slideUp、slideDown、slideToggle、fadeIn、fadeOut、fadeTo、show、hide以及toggle效果以外，jQuery UI还提供了大量全新的视觉特效。这些特效都可以通过调用新的 `effect (effectName, options, duration, callback)` 方法来使用，此方法可作用于 `jQuery ()` 方法返回的jQuery类对象。

