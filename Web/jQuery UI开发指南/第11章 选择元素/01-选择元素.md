### 第11章　选择元素

jQuery UI使得在页面上选择元素变得十分简单。选择（select）在这里的意思是用一个方框把页面上的元素聚集在一起，以便于同时处理（就好像一次选择多个文件，然后将它们一起丢入回收站一样）。比起一个一个地操作（使用我们在第10章学习过的拖放功能），大不如使用鼠标将元素框选起来然后一起拖入回收站。

jQuery UI实现了这一功能，并且操作方式非常类似于图形操作系统Windows、Mac OS或Linux。你可以使用鼠标一次框选多个元素，也可以在按住Ctrl键的同时点击来选择单个元素，或取消某个已经框选的元素。

