[toc]

### 1. 初始化空对象

```js
var q = {};	// 一个空对象
```

### 2. 对象初始化

```js
var p = {
    x: 2.3,
    y: -1.2
};	// 一个拥有两个属性成员的对象
```

或者：

```js
var p = {
    "x": 2.3,
    "y": -1.2
};	// 一个拥有两个属性成员的对象
```

