[toc]

### 1. 使用 charAt() 方法

可以使用字符串的 `charAt()` 方法来获取字符串指定位置的单个字符。例如：

```js
var s = "hello, world";
s.charAt(0);	// => "h"
```

### 2. 使用方括号

也可以使用方括号来访问字符串中的单个字符。例如：

```js
s = "hello, world";
s[0] // => "h"
s[s.length-1] // => "d"
```

