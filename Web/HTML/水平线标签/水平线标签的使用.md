水平线标签属性格式如下：

```html
<hr />
```

水平线标签可以使用如下属性：

+ `size`：水平线宽度。
+ `color`：水平线颜色

```html
<!DOCTYPE html>
<html>
    <head>
        <title>水平线标签</title>
    </head>
    <body>
        <hr size="5" color="blue" />
    </body>
</html>
```

