将整个 `Web` 应用打包为 `WAR` 文件的命令如下：

```shell
jar cvf war文件名 *
```

例如，可以按照如下步骤发布 `helloapp`：

（1）在 `DOS` 中进入 `helloapp` 应用的根目录 `helloapp`。

（2）把整个 `Web` 应用打包为 `helloapp.war` 文件，命令如下：

```shell
C:\helloapp>jar cvf C:\helloapp.war *
```



