

### 
  7.6 小结


在这一章，我们看到了，尽管单页应用的很多逻辑已经移到了客户端，但是服务器仍然承担认证、数据验证和数据存储的职责。我们安装了 Node.js 服务器，使用了 Connect和Express中间件，使得路由、日志输出和认证变得更加容易。

把路由和配置分离到不同的文件，变得容易理解，Express 有为不同的环境定义不同配置的功能。Express提供了很容易创建CRUD路由的工具，它对所有的对象类型都有效。

我们还没有解决如何验证和保存数据的问题，这在下一章会讨论，到时会把应用和数据结合起来。

