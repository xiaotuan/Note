[toc]

在项目中引入 `Vue.js` 有 2 种方式：

+ 使用 `CDN`。
+ 使用 `NPM`。

### 1. 使用 `CDN`

使用 `CDN` 方式来安装 `Vue` 框架，就是选择一个 `Vue.js` 链接稳定的 `CDN` 服务商。选择好 `CDN` 后，在页面中引入 `Vue` 的代码如下:

```html
<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
```

### 2. 使用 npm

`npm` 是一个 `Node.js` 包管理和分发工具，在安装 `Node.js` 环境时，安装包中包含 `npm`，如果安装了 `Node.js`，则不需要再安装 `npm`。

使用 `npm` 安装 `Vue.js 3.x`：

```shell
$ npm install vue@latest

added 20 packages in 6s

3 packages are looking for funding
  run `npm fund` for details
npm notice
npm notice New minor version of npm available! 10.2.4 -> 10.5.0
npm notice Changelog: <https://github.com/npm/cli/releases/tag/v10.5.0>
npm notice Run `npm install -g npm@10.5.0` to update!
npm notice
```





