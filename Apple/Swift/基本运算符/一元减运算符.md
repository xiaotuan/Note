`-` 可以使用称为*一元*减号运算符的前缀来切换数值的符号：

```swift
let three = 3
let minusThree = -three       // minusThree equals -3
let plusThree = -minusThree   // plusThree equals 3, or "minus minus three"
```

一元减号运算符 ( `-` ) 直接放在它所操作的值之前，没有任何空格。