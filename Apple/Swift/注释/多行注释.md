多行注释以一个斜杠加一个星号开头（ `/*` ），以一个星号加斜杠结尾（ `*/` ).

```swift
/* this is also a comment,
but written over multiple lines */
```

Swift 语言中的多行注释可以内嵌在其他的多行注释中，例如：

```swift
/* 这是第一个多行注释的开头
/* 这是第二个嵌套在内的注释块 */
这是第一个注释块的结尾 */
```

