<center><font size="5"><b>文字大小自适应 UILabel 控件大小</b></font></center>

如果改变了 **UILabel**字号，可能需要添加一个自动布局限制，以确保字体在运行时有恰当的大小。为此，选择标签，然后在 **Xcode** 菜单中选择 `Editor -> Size to Fit Content` 或者按下快捷键 <kbd>Command</kbd>+<kbd>=</kbd>。

