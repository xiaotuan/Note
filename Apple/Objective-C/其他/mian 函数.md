接下来看如下代码：

```objc
#import <Foundation/Foundation.h>

int main(int argc, const char * argv[]) {
    @autoreleasepool {
        // insert code here...
        NSLog(@"Hello, World!");
    }
    return 0;
}
```

在 `main` 前面的保留关键字 `int` 用于指定 `main` 返回值的类型。

`@autoreleasepool` 为自动释放池在内存中保留了控件。

