我们已经介绍过 expr 命令，通过它可以处理一些简单的算术命令，但这个命令执行起来相当慢，因为它需要调用一个新的 shell 来处理 expr 命令。

一种更新更好的方法是使用 `$((...))` 扩展。把你准备求值的表达式括在 `$((...))` 中能够更有效地完成简单的算术运算。如下所示：

```shell
#!/bin/sh

x=0
while [ "$x" -ne 10 ]; do
	echo $x
	x=$(($x + 1))
done

exit 0
```

