Linux 下的格式化磁盘命令为 `mkfs`，命令格式如下：

```shell
mkfs [参数] [-t 文件系统类型] [分区名称]
```

主要参数如下：

+ `fs`：指定建立文件系统时的参数。
+ `-V`：显示版本信息和简要的使用方法。
+ `-v` 显示版本信息和详细的使用方法。

比如我们要格式化 U 盘的分区 `/dev/sdb1` 为 FAT 格式，那么就可以使用如下命令：

```shell
$ mkfs -t vfat /dev/sdb1
```

