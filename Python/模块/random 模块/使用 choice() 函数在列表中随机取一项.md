`choice()` 函数用于在给定的列表参数中随机获取列表中的一项：

```python
import random

y = random.choice([ 'apple', 'banana', 'cherry', 'durian' ])
print(y)
```

运行结果如下：

```shell
$ python .\Test.py
banana
$ python .\Test.py
durian
```

