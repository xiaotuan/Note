[toc]

### 1. 检查相等

可以使用 `==` 运算符检查两个变量是否相等：

```python
>>> car = 'bmw'
>>> car == 'bmw'
True
```

### 2. 检查是否相等时不考虑大小写

可以先将变量转换成小写，再与小写字符串进行比较即可：

```python
>>> car = 'Audi'
>>> car.lower() == 'audi'
True
```

