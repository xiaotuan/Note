函数定义格式如下：

```python
def 函数名(参数1, 参数2, ..., 参数n):
    函数体
```

例如：

```python
def greet_user():
	"""显示简单的问候语"""
	print("Hello!")
	
greet_user()
```

或

```python
def greet_user(username):
	"""显示简单的问候语"""
	print("Hello, " + username.title() + "!")
	
greet_user('jesse')
```

