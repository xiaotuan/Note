可以使用 `for` 循环遍历列表中的元素，代码格式如下所示：

```python
for 元素 in 列表:
    对元素进行的操作
```

例如：

```python
bob = ['Bob Smith', 42, 30000, 'software']
sue = ['Sue Jones', 45, 40000, 'hardware']

people = [bob, sue]

for person in people:
    print(person)
```

