可以使用 `-1` 下标来获取列表中的最后一个元素。例如：

```python
bob = ['Bob Smith', 42, 30000, 'software']
bob[-1]
// 'software'
```

同理，获取列表中倒数第 n 个元素的值，可以使用 `-n` 作为列表的下标。例如获取列表中倒数第 2 个元素的方法如下所示：

```python
bob[-2]
// 30000
```

