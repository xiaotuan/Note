函数 `input()` 让程序暂停运行，等待用户输入一些文本。例如：

```python
message = input("Tell me something, and I will repeat it back to you: ")
print(message)
```

> 提示：函数 `input()` 接受一个参数：即要向用户显示的提示或说明，让用户知道该如何做。

> 注意：使用函数 `input()` 时，Python 将用户输入解读为字符串。