### 14.1　安装和设置EZSheets

可以通过打开一个新的命令行窗口并运行 `pip install --user ezsheets` 来安装EZSheets。安装EZSheets时也会安装 `google-api-python-client` 、 `google-auth-httplib2` 和 `google-auth-oauthlib` 模块。这些模块允许你的程序登录到Google的服务器上，并提出API请求。EZSheets会处理与这些模块的交互，因为你不需要关心它们是如何工作的。

