### 5.5　习题

1．空字典的代码是什么样的？

2．一个字典包含键 `'fow'` 和值42，它看起来是什么样的？

3．字典和列表的主要区别是什么？

4．如果 `spam` 变量是 `{'bar': 100}` ，那么当你试图访问 `spam['foo']` 时，会发生什么？

5．如果一个字典保存在 `spam` 变量中，那么表达式 `'cat' in spam` 和 `'cat' in spam.keys()` 之间的区别是什么？

6．如果一个字典保存在 `spam` 变量中，那么表达式 `'cat' in spam` 和 `'cat' in spam.values()` 之间的区别是什么？

7．下面代码的简洁写法是什么？

```javascript
if 'color' not in spam:
    spam['color'] = 'black'
```

8．什么模块和函数可以用于输出美观的字典值？

