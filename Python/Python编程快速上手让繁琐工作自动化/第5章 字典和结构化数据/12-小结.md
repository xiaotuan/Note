### 5.4　小结

在本章中，你学习了字典的所有相关知识。列表和字典可以包含多个值，当然，可以包括其他列表和字典，它们本身也是一个值。字典是有用的，因为你可以把一些项（键）映射到另一些项（值），不像列表只包含一系列有序的值。字典中的值是通过方括号访问的，像列表一样。字典不使用整数索引，而是用各种数据类型如整型、浮点型、字符串或元组作为键。通过将程序中的值组织成数据结构，你可以创建真实世界事物的模型，井字棋盘就是这样一个例子。

