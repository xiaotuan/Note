### 1.8　习题

1．下面哪些是操作符，哪些是值？

```javascript
*
'hello'
-88.8
-
/
+
5
```

2．下面哪个是变量，哪个是字符串？

```javascript
spam
'spam'
```

3．说出3种数据类型。

4．表达式由什么构成？所有表达式都做什么事？

5．本章介绍了赋值语句，如 `spam = 10` 。表达式和语句有什么区别？

6．下列语句运行后，变量 `bacon` 的值是什么？

```javascript
bacon = 20
bacon + 1
```

7．下面两个表达式求值的结果是什么？

```javascript
'spam' + 'spamspam'
'spam' * 3
```

8．为什么 `eggs` 是有效的变量名，而 `100` 是无效的变量名？

9．哪3个函数能分别取得一个值的整型、浮点型和字符串形式？

10．为什么下面这个表达式会导致错误？如何修复？

```javascript
'I have eaten ' + 99 + ' burritos.'
```

附加题：在线查找 `len()` 函数的Python文档。查看Python的其他函数的列表，查看 `round()` 函数的功能，并在交互式环境中使用它。



