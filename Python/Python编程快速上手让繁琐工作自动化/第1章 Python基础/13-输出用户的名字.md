### 1.6.4　输出用户的名字

接下来的 `print()` 调用，实际上在括号中包含了表达式 `'It is good to meet you, ' + myName` ：

```javascript
❹ print('It is good to meet you, ' + myName)
```

要记住，表达式总是可以求值为一个值。如果 `'Al'` 是上一行代码保存在 `myName` 中的值，那么这个表达式就求值为 `'It is good to meet you, Al'` 。这个字符串传给 `print()` ，它将输出到屏幕上。

