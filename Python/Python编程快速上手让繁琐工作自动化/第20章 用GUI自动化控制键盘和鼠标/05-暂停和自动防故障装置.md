### 20.3.1　暂停和自动防故障装置

如果你的程序出现错误，无法使用键盘和鼠标关闭程序，你可以使用PyAutoGUI的故障安全功能，快速地将鼠标指针滑动到屏幕的4个角之一。每个PyAutoGUI函数调用在执行动作后都有1/10秒的延迟，以便让你有足够的时间将鼠标指针移动到一个角落。如果PyAutoGUI随后发现鼠标指针在角落里，会引发 `pyautogui. FailSafeException` 异常。非PyAutoGUI指令不会有这个1/10秒的延迟。

如果你发现自己需要停止PyAutoGUI程序，只需将鼠标指针移向角落即可。

