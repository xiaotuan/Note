### 9.8　习题

1．相对路径是相对于什么？

2．绝对路径从什么开始？

3．在Windows操作系统上， `Path('C:/ Users')/'Al'` 的求值结果是什么？

4．在Windows操作系统上， `'C:/Users' / 'Al'` 的求值结果是什么？

5． `os.getcwd()` 和 `os.chdir()` 函数做什么事？

6．.和..文件夹是什么？

7．在C:\bacon\eggs\spam.txt中，哪一部分是目录名称，哪一部分是基本名称？

8．可以传递给 `open()` 函数的3种“模式”参数是什么？

9．如果已有的文件以写模式打开，会发生什么？

10． `read()` 和 `readlines()` 方法之间的区别是什么？

11． `shelf` 值与什么数据结构相似？

