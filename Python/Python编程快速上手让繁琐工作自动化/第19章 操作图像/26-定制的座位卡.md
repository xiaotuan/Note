### 19.7.3　定制的座位卡

第15章包含了一个实践项目，利用纯文本文件的客人名单来创建定制的邀请函。作为附加项目，请使用 `pillow` 模块为客人创建定制的座位卡图像。从异步社区本书对应页面中下载资源文件guests.txt，对于其中列出的客人，生成带有客人名字和一些鲜花装饰的图像文件。在本书提供的资源中，包含一个版权为公共领域的鲜花图像。

为了确保每个座位卡大小相同，在图像的边缘添加一个黑色的矩形，这样在图像输出时，可以沿线裁剪。 `pillow` 生成的PNG文件被设置为每英寸72个像素，因此4英寸×5英寸的卡片需要使用288像素×360像素的图像。



