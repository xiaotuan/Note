### 7.16　小结

虽然计算机可以很快地查找文本，但你必须精确地告诉它要找什么。正则表达式让你可以精确地指明要找的文本模式。实际上，某些文字处理和电子表格应用也提供了查找替换功能，让你使用正则表达式进行查找。

Python自带的 `re` 模块可让你编译 `Regex` 对象。该对象有几种方法： `search()` 方法查找单词匹配， `findall()` 方法查找所有匹配实例， `sub()` 方法对文本进行查找和替换。

你可以在Python官方文档中找到更多内容。

