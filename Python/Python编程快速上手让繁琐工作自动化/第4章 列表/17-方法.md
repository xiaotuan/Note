### 4.4　方法

“方法”和函数是一回事，只是它在一个值上进行调用。例如，如果一个列表值存储在 `spam` 变量中，你可以在这个列表上调用 `index()` 列表方法（稍后我会解释），就像 `spam.index('hello')` 一样。方法跟在要调用的值后面，以一个圆点分隔。

每种数据类型都有它自己的一组方法。例如，列表数据类型有一些有用的方法，用来查找、添加、删除或修改列表中的值。

