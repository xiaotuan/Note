表示择一匹配的管道符号（`|`），表示一个 "从多个模式中选择其一" 的操作。它用于分割不同的正则表达式。例如：

| 正则表达式模式 | 匹配的字符串        |
| -------------- | ------------------- |
| `at|home`      | `at`、`home`        |
| `r2d2|c3po`    | `r2d2`、`c3po`      |
| `bat|bet|bit`  | `bat`、`bet`、`bit` |

