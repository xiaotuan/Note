1. 单行注释

   ```js
   // 1. 单行注释
   ```

   > 提示： vscode 中对应的快捷键为：<kbd>Ctrl</kbd> + <kbd>\\</kbd>

2. 多行注释

   ```js
   /* 2. 多行注释 默认的快捷键 shift + alt + a
      2. 多行注释
    */
   ```

   > 提示：vscode 中对应的快捷键为：<kbd>Shift</kbd> + <kbd>Alt</kbd> + <kbd>A</kbd>。



**index.html**

```html
<!DOCTYPE html>
<html lang="zh">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Document</title>
        <style></style>
        <script>
            // 1. 单行注释 ctrl+ /
            /* 2. 多行注释 默认的快捷键 shift + alt + a
               2. 多行注释
             */
        </script>
    </head>
    <body>
    </body>
</html>
```



