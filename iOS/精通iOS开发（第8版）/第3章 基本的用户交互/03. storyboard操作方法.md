<center><font size="5"><b>storyboard 操作方法</b></font></center>

1. 除了能从库中拖出一个新对象，也可以按住<kbd>Option</kbd>键不放并且拖动想要复制的对象一生成副本。
2. 如果有不可见的用户界面元素，比如空白标签，而你又希望看到它们所在的位置，可以从 `Editor` 菜单中选择 `Canvas` ， 然后从弹出的子菜单中勾选 `Show Bounds Rectangles`。