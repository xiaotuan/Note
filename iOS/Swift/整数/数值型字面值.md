整数型字面量可以写作：

+   一个十进制数，没有前缀。
+   一个二进制数，前缀是 `0b`。
+   一个八进制数，前缀是 `0o`。
+   一个十六进制，前缀是 `0x`。

例如：

```swift
let decimalInteger = 17
let binaryInteger = 0b10001
let octalInteger = 0o21
let hexadecimalInteger = 0x11
```

