### 11.2.3　多机覆盖网络

后面本书会有专门的章节去介绍多机覆盖网络，所以本节内容尽量做到简洁。

覆盖网络适用于多机环境。它允许单个网络包含多个主机，这样不同主机上的容器间就可以在链路层实现通信。覆盖网络是理想的容器间通信方式，支持完全容器化的应用，并且具备良好的伸缩性。

Docker为覆盖网络提供了本地驱动。这使得创建覆盖网络非常简单，只需要在 `docker network create` 命令中添加 `--d overlay` 参数。

