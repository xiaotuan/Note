### 第23章　定时器与休眠

定时器是进程规划自己在未来某一时刻接获通知的一种机制。休眠则能使进程（或线程）暂停执行一段时间。本章讨论了定时器设置以及休眠的接口，涵盖主题如下。

+ 针对间隔式定时器设置的传统UNIX API（setitimer()和alarm()），一经设定，会在特定的一段时间后通知进程。
+ 允许进程休眠特定时间的API接口。
+ POSIX.1b时钟和定时器API接口。
+ Linux特有的timerfd功能，允许所创建定时器的到期信息可从文件描述符中读取。

