`Swift` 提供了两种有符号浮点数类型：

+   `Double` 表示一个 64 位浮点数。
+   `Float` 表示一个 32 位浮点数。

>   提示：
>
>   `Double` 具有至少 15 位十进制数字的精度，而 `Float` 的精度低至 6 位十进制数字。要使用的浮点类型取决于您需要再代码中使用的值的性质和范围。在任何一种类型都合适的情况下，首选 `Double` 类型。

