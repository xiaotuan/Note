Visual Basic 能够获取当前的系统日期和时间，这是通过 `DateTime` 结构实现的。要将当前的系统日期赋给一个新的 `Date` 变量，可用如下语句：

```vb
Dim dteToday As Date = DateTime.Today
```

要获得当前的系统日期和时间，可使用 `DateTime` 的 `Now` 属性，如下所示：

```vb
Dim dteToday As Date = DateTime.Now
```

