在 `工具箱` -> `组件` 中双击 `Timer` 控件，将控件添加到设计窗口中。

![01](./images/01.png)

> 注意：`Timer` 控件在运行时是不可见的，因此它被添加到屏幕底部的灰色区域中，而不是窗体。
>
> ![02](./images/02.png)

`Timer` 控件最重要的属性是 `Interval` 属性，该属性决定了 `Timer` 控件每隔多长时间触发其 `Tick` 事件。`Interval` 属性以毫秒为单位。