`Len()` 接受一个字符串（变量或字面量）并返回字符串中包含的字符数。

```vb
Debug.WriteLine(Len("Pink Floyd reigns supreme."))
```

> 提示：要获悉字符串变量包含的字符数，另一种办法是使用变量的 Length 属性，如下所示：
>
> ```vb
> Debug.WriteLine(strMyStringVariable.Length())
> ```

