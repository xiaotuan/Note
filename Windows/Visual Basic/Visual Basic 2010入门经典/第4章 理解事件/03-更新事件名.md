你知道，事件过程的名称由控件名、下划线和事件名组成，如 txtAddress_TextChanged。用户修改控件的名称时，Visual Basic 不会根据控件的新名称相应地修改其事件声明，但确保事件与控件相关联。这是因为每个事件声明的末尾都有关键字 Handles，然后是对象名；关键字 Handles 将事件处理程序同控件的事件关联起来。修改控件名时，Visual Basic 将修改 Handles 引用，但不修改事件名本身。虽然代码仍能够运行，但应该手工修改相应过程的名称，使其同新的控件名匹配。