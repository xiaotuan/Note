下表列出了 [System.Math](https://learn.microsoft.com/zh-cn/dotnet/api/system.math) 类的方法。 你可以在 Visual Basic 程序中使用以下函数：

| .NET 方法                                                    | 说明                                                         |
| :----------------------------------------------------------- | :----------------------------------------------------------- |
| [Abs](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.abs) | 返回某一数字的绝对值。                                       |
| [Acos](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.acos) | 返回余弦值为指定数字的角度。                                 |
| [Asin](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.asin) | 返回正弦值为指定数字的角度。                                 |
| [Atan](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.atan) | 返回正切值为指定数字的角度。                                 |
| [Atan2](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.atan2) | 返回正切值为两个指定数字的商的角度。                         |
| [BigMul](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.bigmul) | 返回两个 32 位数的完整乘积。                                 |
| [Ceiling](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.ceiling) | 返回大于或等于指定 `Decimal` 或 `Double` 的最小整数值。      |
| [Cos](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.cos) | 返回指定角度的余弦值。                                       |
| [Cosh](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.cosh) | 返回指定角度的双曲余弦值。                                   |
| [DivRem](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.divrem) | 返回两个 32 位或 64 位有符号整数的商，并通过输出参数返回余数。 |
| [Exp](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.exp) | 返回 e（自然对数的底）的指定幂次方。                         |
| [Floor](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.floor) | 返回小于或等于指定 `Decimal` 或 `Double` 的最大整数。        |
| [IEEERemainder](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.ieeeremainder) | 返回一指定数字被另一指定数字相除的余数。                     |
| [Log](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.log) | 返回指定数字的自然（以 e 为底）对数或指定基数中指定数字的对数。 |
| [Log10](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.log10) | 返回指定数字以 10 为底的对数。                               |
| [Max](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.max) | 返回两个数字中的较大者。                                     |
| [Min](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.min) | 返回两个数字中较小的一个。                                   |
| [Pow](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.pow) | 返回指定数字的指定次幂。                                     |
| [Round](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.round) | 返回四舍五入到最接近的整数值或指定小数位数的 `Decimal` 或 `Double` 值。 |
| [Sign](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.sign) | 返回表示数字符号的 `Integer` 值。                            |
| [Sin](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.sin) | 返回指定角度的正弦值。                                       |
| [Sinh](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.sinh) | 返回指定角度的双曲正弦值。                                   |
| [Sqrt](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.sqrt) | 返回指定数字的平方根。                                       |
| [Tan](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.tan) | 返回指定角度的正切值。                                       |
| [Tanh](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.tanh) | 返回指定角度的双曲正切值。                                   |
| [Truncate](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.truncate) | 计算指定 `Decimal` 或 `Double` 数字的整数部分。              |

下表列出了 .NET Framework 中不存在但在 .NET Standard 或 .NET Core 中新增加的 [System.Math](https://learn.microsoft.com/zh-cn/dotnet/api/system.math) 类的方法：

| .NET 方法                                                    | 说明                                                   | 适用于                                     |
| :----------------------------------------------------------- | :----------------------------------------------------- | :----------------------------------------- |
| [Acosh](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.acosh) | 返回双曲余弦值为指定数字的角度。                       | 从 .NET Core 2.1 和 .NET Standard 2.1 开始 |
| [Asinh](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.asinh) | 返回双曲正弦值为指定数字的角度。                       | 从 .NET Core 2.1 和 .NET Standard 2.1 开始 |
| [Atanh](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.atanh) | 返回双曲正切值为指定数字的角度。                       | 从 .NET Core 2.1 和 .NET Standard 2.1 开始 |
| [BitDecrement](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.bitdecrement) | 返回小于 `x` 的下一个最小值。                          | 从 .NET Core 3.0 开始                      |
| [BitIncrement](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.bitincrement) | 返回大于 `x` 的下一个最大值。                          | 从 .NET Core 3.0 开始                      |
| [Cbrt](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.cbrt) | 返回指定数字的立方根。                                 | 从 .NET Core 2.1 和 .NET Standard 2.1 开始 |
| [Clamp](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.clamp) | 返回限制在 `min` 和 `max` 范围内（含首尾）的 `value`。 | 从 .NET Core 2.0 和 .NET Standard 2.1 开始 |
| [CopySign](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.copysign) | 返回一个值，它具有 `x` 的大小和 `y` 的符号。           | 从 .NET Core 3.0 开始                      |
| [FusedMultiplyAdd](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.fusedmultiplyadd) | 返回 (x * y) + z，舍入为一个三元运算。                 | 从 .NET Core 3.0 开始                      |
| [ILogB](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.ilogb) | 返回指定数字以 2 为底的整数对数。                      | 从 .NET Core 3.0 开始                      |
| [Log2](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.log2) | 返回指定数字以 2 为底的对数。                          | 从 .NET Core 3.0 开始                      |
| [MaxMagnitude](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.maxmagnitude) | 返回两个双精度浮点数字中的较大值。                     | 从 .NET Core 3.0 开始                      |
| [MinMagnitude](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.minmagnitude) | 返回两个双精度浮点数字中的较小值。                     | 从 .NET Core 3.0 开始                      |
| [ScaleB](https://learn.microsoft.com/zh-cn/dotnet/api/system.math.scaleb) | 返回有效计算的 x * 2^n。                               | 从 .NET Core 3.0 开始                      |

要在没有限定的情况下使用这些函数，请将以下代码添加到源文件的顶部，从而将 [System.Math](https://learn.microsoft.com/zh-cn/dotnet/api/system.math) 命名空间导入到你的项目中：

```vb
Imports System.Math
```

例如：

```vb
Dim x As Double = Math.Abs(50.3)
Dim y As Double = Math.Abs(-50.3)
Console.WriteLine(x)
Console.WriteLine(y)
' This example produces the following output:
' 50.3
' 50.3

Public Function GetPi() As Double
    ' Calculate the value of pi.
    Return 4.0 * Math.Atan(1.0)
End Function

Public Function Sinh(angle As Double) As Double
    ' Calculate hyperbolic sine of an angle, in radians.
    Return (Math.Exp(angle) - Math.Exp(-angle)) / 2.0
End Function

Public Function Asinh(value As Double) As Double
    ' Calculate inverse hyperbolic sine, in radians.
    Return Math.Log(value + Math.Sqrt(value * value + 1.0))
End Function

Dim myVar2 As Double = Math.Round(2.8)
Console.WriteLine(myVar2)
' The code produces the following output:
' 3

Dim mySign1 As Integer = Math.Sign(12)
Dim mySign2 As Integer = Math.Sign(-2.4)
Dim mySign3 As Integer = Math.Sign(0)
Console.WriteLine(mySign1)
Console.WriteLine(mySign2)
Console.WriteLine(mySign3)
' The code produces the following output:
' 1
' -1
' 0

Public Function Csc(angle As Double) As Double
    ' Calculate cosecant of an angle, in radians.
    Return 1.0 / Math.Sin(angle)
End Function

Dim mySqrt1 As Double = Math.Sqrt(4)
Dim mySqrt2 As Double = Math.Sqrt(23)
Dim mySqrt3 As Double = Math.Sqrt(0)
Dim mySqrt4 As Double = Math.Sqrt(-4)
Console.WriteLine(mySqrt1)
Console.WriteLine(mySqrt2)
Console.WriteLine(mySqrt3)
Console.WriteLine(mySqrt4)
' The code produces the following output:
' 2
' 4.79583152331272
' 0
' NaN

Public Function Ctan(angle As Double) As Double
    ' Calculate cotangent of an angle, in radians.
    Return 1.0 / Math.Tan(angle)
End Function
```

