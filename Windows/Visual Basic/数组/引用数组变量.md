要给数组变量的第一个元素赋值，可以使用 0 作为索引，如下所示：

```vb
strMyArray(0) = "This value goes in the irst element"
```

要引用第二个元素，可使用如下所示的语句：

```vb
strMyVariable = strMyArray(1)
```

> 提示：大多数计算机操作都将 0 而不是 1 作为序列中的第一个数，数组索引也是这样的。