窗体或控件可以与受到限制的关键字同名。例如，可以将某个控件命名为 If，但在代码中不能用通常的方法引用该控件，因为在 VB 中 If 意味着关键。例如：

```vb
If.Caption = "同意" ' 出错
```

为了引用那些与受到限制的关键字同名的窗体或控件，就必须限定它们，或者将其用方括号 `[]` 括起来。例如：

```vb
MyForm.If.Caption = "同意"	' 用窗体名将其限定
[If].Caption = "同意"			' 方括号起了作用
```

