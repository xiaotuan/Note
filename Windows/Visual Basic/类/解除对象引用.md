对对象的最后一个引用被解除时，对象将自动销毁。释放对象引用的一种方法是在存储对象引用的对象变量超出其作用域。然而，让对象变量超出其作用域时，对象不一定完全销毁，对象占用的内存也不一定会完全释放。因此，依赖于作用域来释放对象不是个好办法。

要显示地释放对象，将对象变量设置为 `Nothing`，如下所示：

```vb
objMyObject = Nothing
```

将对象变量设置为 `Nothing`，可保证对象引用完全释放。然而，如果还有其他变量引用该对象，该对象将不会销毁！最后一个引用被释放后，垃圾回收器将销毁对象。