如果需要更换窗体的图标，可以在 `属性` 面板中设置窗体的 `Icon` 属性。

（1）选中窗体，然后在窗体的 `属性` 面板中选中 `Icon` 属性，会出现 <img src="./images/04.png" /> 按钮。

![05](./images/05.png)

> 注意：在添加窗体图标时，其图片格式只能是 `ico`。

（2）单击 <img src="./images/04.png"/> 按钮，打开选择图标文件对话框。

（3）选择新的窗体图标文件之后，单击 `打开` 按钮，完成窗体图标的更换。