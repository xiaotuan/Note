`.NET Standard` 不是一个实现，而是一个协定。本协定规定了需要实现哪些 `API`。在每个新的 `.NET Standard` 版本中都添加了新的 `API`。但 `API` 从不会被删除。

可以在 <https://learn.microsoft.com/zh-cn/dotnet/standard/net-standard?tabs=net-standard-1-0> 中找到 `.NET Standard` 的平台支持列表。