`if` 语句的语法格式如下所示：

```c#
if (布尔表达式)
{
    语句序列
}
```

若语句块中仅有一条语句，则可以省略条件语句中的 `{ }`。

> 提示：虽然 `if` 后面的语句序列块只有一条语句，省略 `{}` 并无语法错误，但为了增强程序的可读性最好不要省略。

例如：

```c#
int i = 928;
if (i > 927)
{
    Console.WriteLine("i 大于 927");
}
```

