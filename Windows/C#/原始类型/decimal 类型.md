`decimal` 类型表示 128 位数据类型，它是一种精度更高的浮点类型，其精度可以达到 28 位，取值范围为 ±1.0 x 10<sup>-28</sup> ~ ±7.9 x 10<sup>28</sup>。

由于 `decimal` 类型的高精度特性，它更适合于财务和货币计算。如果希望一个小数被当成 `decimal` 类型使用，需要使用后缀 `m` 或 `M`，例如：

```c#
decimal myMoney = 1.12m;
```

