[toc]

### 1. 字符串常量

在 C# 语言中，字符串必须包含在一对 `""`（双引号）之内。例如：

```c#
"23.23"
"ABCDEF"
"你好"
```

> 注意：在 C# 中由双引号（`""`）包围的都是字符串，不能作为其他数据类型来使用，如 "1+2" 的输出结果永远也不会是 3。

### 2. 声明字符串变量

可以通过以下语法格式来声明字符串变量：

```c#
string str;
```

声明字符串之后，需要对其进行赋值，字符串的赋值使用 "="，例如：

```c#
string str = "C#编程词典";
```

