在默认情况下，`Excel` 每次新建工作簿，其中包含的工作表数量是 1 个。`Excel` 允许调整这个数值，在 `选项` 对话框的 `常规` 选项卡中，在 `新建工作簿时` 下有个 `包含的工作表数(S)` 数值调节按钮，调整范围在 1 ~ 255 之间。

另外，还可以调整新工作簿内标准字体的大小。方法是，调整 `选项` 对话框的 `常规` 选项卡中 `字号` 数值调节按钮的值。字号的调整需要重新启动 `Excel` 才生效。

![12](./images/12.png)