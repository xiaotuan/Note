### 14.3.3　Repeater 控件

RepeaterWeb服务器控件是一个容器控件，它使您可以从页的任何可用数据中创建出自定义列表。Repeater控件不具备内置的呈现功能，这表示用户必须通过创建模板为Repeater控件提供布局。当该页运行时，Repeater控件依次通过数据源中的记录，并为每个记录呈现一个项。

Repeater控件与上述2个控件相比显得最为单薄，Repeater一般用于只读的数据，对数据呈现的列表不具备编辑、排序、分页等功能。

适用场景：功能非常单薄，它使用数据源返回的一组记录呈现只读列表。

