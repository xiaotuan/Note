<center><font size="5"><b>scroll-view 的使用说明</b></font></center>

1. 虽然 `scroll-view` 是滚动组件，但是在没有为其设置 `scroll-x` 或 `scroll-y` 属性时，它是不能够滚动的。且如果设置 `scroll-x` 时 `width` 不能设置为 100%；如果设置 `scroll-y` 时 `height` 不能设置为 100%。因为设置为 100% 后表示设置组件的宽高为完全包含内容的宽高。