<center><font size="5"><b>全局样式（app.wxss）</b></font></center>

`app.wxss` 是全局样式表，对项目中每个页面都有效，可将一些系统级别的统一样式风格写入这个文件，页面渲染时，框架页中的 .wxs 文件样式会覆盖 `app.wxss` 中相同的选择器样式。 WXSS 是小程序基于 CSS 拓展的一套样式语言，它实现了 CSS 大部分规则。